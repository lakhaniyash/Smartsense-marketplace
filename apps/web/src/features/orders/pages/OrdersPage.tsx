import { ComingSoonPage } from '@shared/components'

// M8 proves the routing/guard infrastructure only — the real Orders UI
// ships with M13 (docs/milestones.md).
export function OrdersPage() {
  return <ComingSoonPage title="Orders" />
}
