query GetRevenueReport($filter: RevenueReportFilterInput) {
  revenueReport(filter: $filter) {
    invoiceCount
    totalCommission
    totalGrossRevenue
    totalNetPayout
    trend {
      bucketStart
      bucketEnd
      grossRevenue
      invoiceCount
    }
  }
}
