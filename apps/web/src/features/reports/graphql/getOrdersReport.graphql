query GetOrdersReport($filter: OrdersReportFilterInput) {
  ordersReport(filter: $filter) {
    totalOrders
    totalRevenue
    averageOrderValue
    statusBreakdown {
      status
      count
    }
  }
}
