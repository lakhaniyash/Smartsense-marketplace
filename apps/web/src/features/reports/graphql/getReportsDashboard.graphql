query GetReportsDashboard($filter: ReportsDashboardFilterInput) {
  reportsDashboard(filter: $filter) {
    grossRevenue
    ordersRevenue
    totalOrders
    lowStockCount
    revenueTrend {
      bucketStart
      bucketEnd
      grossRevenue
      invoiceCount
    }
  }
}
