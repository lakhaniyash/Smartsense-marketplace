import { ComingSoonPage } from '@shared/components'

// M8 proves the routing/guard infrastructure only — the real Catalog UI
// ships with M12 (docs/milestones.md).
export function CatalogPage() {
  return <ComingSoonPage title="Catalog" />
}
