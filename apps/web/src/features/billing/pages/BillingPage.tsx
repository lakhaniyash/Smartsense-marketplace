import { ComingSoonPage } from '@shared/components'

// M8 proves the routing/guard infrastructure only — the real Billing UI
// ships with M14 (docs/milestones.md).
export function BillingPage() {
  return <ComingSoonPage title="Billing" />
}
